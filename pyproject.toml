[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "supermag"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "certifi",
  "pandas",
  "urllib3"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["supermag*"]

[project.scripts]
supermag-inventory = "supermag.inventory:main"
supermag-locations = "supermag.locations:main"
supermag-data      = "supermag.data:main"
